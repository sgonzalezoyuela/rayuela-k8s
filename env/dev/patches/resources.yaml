# Dev environment: smaller resources, single replica
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rayuela
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: rayuela
          resources:
            requests:
              cpu: 1000m
              memory: 512Mi
            limits:
              cpu: 2000m
              memory: 1Gi
