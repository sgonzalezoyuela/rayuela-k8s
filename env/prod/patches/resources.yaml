# Prod environment: larger resources, multiple replicas
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rayuela
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: rayuela
          resources:
            requests:
              cpu: 1000m
              memory: 1Gi
            limits:
              cpu: 2000m
              memory: 2Gi
